2026-09-18: Alterações no backend do CPI Adopt

- backend/app/cisco/cpi_adopt_router.py (linhas 1-167, com destaque para 68-93 e 114-145)
  - novo router/endpoints para o report CPI Adopt
  - endpoints criados: report, filters, summary, fy-range e cache/invalidate
  - proteção dos endpoints com autenticação via token
  - proteção dos endpoints no backend com a permissão cisco.cpi_adopt, seguindo o padrão de autorização do sistema
  - restrição do endpoint de invalidação de cache para ADMIN
  - adição dos novos query params task_id, task_ws e deal_id nos endpoints report e summary

- backend/app/cisco/cpi_adopt_service.py (linhas 1-248, com destaque para 91-107, 111-122 e 129-142)
  - implementação da lógica de negócio do CPI Adopt Report
  - carregamento de dados com cache TTL
  - filtros de client, solution, task_status, opt_in, project, timeline_stage, fy_start e fy_end
  - consolidação da lógica de timeline no backend para suportar os estágios Early, Mid, Late e Completed usados pelo filtro do report
  - revisão das regras de summary/KPIs para alinhamento conceitual com as linhas retornadas ao frontend
  - adição dos novos filtros textuais task_id, task_ws e deal_id na assinatura do serviço e na aplicação dos filtros em memória

- backend/migrations/add_cisco_cpi_adopt_resource.sql (linhas 1-20, com destaque para 10-17 e 19-20)
  - criação da migration para cadastro do recurso de permissão cisco.cpi_adopt
  - insert idempotente em tbAuthResource com show_in_menu = 1
  - query de verificação do recurso após execução

- backend/app/main.py
  - inclusão/registro do router do CPI Adopt na aplicação
  - número exato de linhas não confirmado nesta revisão; registrar referência no próximo ajuste se necessário

2026-09-22: Ajustes no backend de Tasks

- backend/app/tasks/filter_service.py
  - inclusão de fallback SQL em get_filter_options() para consultar vwFilterTask diretamente quando repositories não estiverem disponíveis
  - criação do helper _sorted_unique_from_rows() para montar listas únicas/sortidas sem dependência de DataFrame
  - registro de diagnóstico de ambiente indicando falha de dependências locais (pandas/mysql), afetando a carga inicial de filter-options

2026-09-22: Ajustes adicionais no backend

- backend/app/main.py
  - inclusão/registro do router do CPI Adopt na aplicação

- backend/app/modules/public_router.py
  - criação do endpoint de download bruto de failed rows para importações do módulo Public > Importer

- backend/app/modules/public_service.py
  - adição de fallback SQL para carga de dados de Public > CSM Account quando repository não estiver disponível
  - implementação de resolução robusta do arquivo de failed rows a partir da mensagem da importação, convenção de nome e busca por stem
  - criação do helper get_failed_rows_file() para suportar download direto do arquivo físico

- backend/app/modules/sections_service.py
  - persistência direta em tbProjectTeam para inclusão/edição/remoção de membros quando o ProjectRepository não oferece operações de escrita correspondentes
  - ajuste do save_project() para usar o entrypoint insert/upsert disponível no ProjectRepository
  - correção do carregamento de Team Goals no Admin para retornar o dataset completo, inclusive sem filtro de FY

2026-09-23: Ajuste no script de ambiente local

- manage_bridgeadoption.ps1
  - correção da montagem de DATABASE_URL no comando PowerShell do backend usando `${variavel}` para evitar erro de parser em trechos como `usuario:senha` dentro da string

2026-09-23: Ajuste adicional no backend de Tasks

- backend/app/tasks/filter_service.py
  - inclusão de fallback SQL em filter_tasks() para consultar vwTask diretamente quando os repositories locais não estiverem disponíveis, evitando retorno vazio na aba Tasks > Filter mesmo com opções preenchidas
  - compatibilização do filtro de status no fallback para aceitar tanto task_status_name quanto task_status_reclassified
  - ajuste adicional para preservar matches encontrados em vwFilterTask mesmo quando o enriquecimento posterior em vwTask não retornar linhas correspondentes
  - fallback secundário do caminho SQL para consultar vwFilterTask quando vwTask vier vazia, mantendo consistência entre os valores exibidos nos selects e os resultados filtrados
  - reforço da carga de `task_types` para o filtro de Tasks com estratégia mais robusta de leitura dos tipos cadastrados, deduplicação, normalização de espaços e fallback SQL direto, corrigindo ausência de tipos válidos no dropdown `TASK TYPE`

- src/domain/i18n/translator.py
  - substituição do path absoluto Linux por resolução relativa a `__file__`, permitindo localizar corretamente os arquivos de idioma em ambiente local/Windows
  - adição de fallback de idioma curto/default (`en-US` → `en`, `pt-BR` → `pt`, etc.), evitando quebra de `get_filter_options()` por `Idioma não suportado`

2026-09-24: Ajustes no backend de Adoption > Cisco LCI > Eligible Status

- backend/app/adoption/lci_eligible_status_service.py
  - correção da preparação dos dados do report Eligible Status para compatibilizar as colunas de data vindas de `vwCiscoLCI` (`lci_stage_start_date` / `lci_stage_end_date`) com os nomes esperados pelo serviço (`lci_stage_start` / `lci_stage_end`)
  - correção do agrupamento mensal do gráfico Executive Monthly View, que estava ficando sem dados por causa da ausência de datas normalizadas
  - reforço de robustez em `get_lci_eligible_status_execution_detail()` para tolerar pequenas variações de shape/colunas do dataset e evitar retorno vazio na tabela `Stage Execution Detail`
  - inclusão de fallbacks adicionais para montagem do mês/ordenação usando `lci_stage_end_date` e `lci_stage_start_date` quando necessário

2026-09-24: Ajustes no backend de Portfolio > Projects

- backend/app/modules/sections_service.py
  - reimplementação de `get_projects()` para leitura direta via SQL em `vwProject`, eliminando a dependência de `ProjectRepository.get_project(..., as_df=True)` no carregamento da tela Projects
  - manutenção da busca por OV com SQL direto em `tbProjectOV` + `vwProject` dentro do mesmo fluxo
  - correção da carga de projetos por `customer_id`, restaurando o preenchimento da grade e das opções do `OV FILTER`

2026-09-24: Ajustes no backend de Public > CSM Account

- backend/app/modules/public_service.py
  - alteração de `get_public_csm_account()` para reutilizar prioritariamente `app.adoption.csm_account_service.get_csm_accounts()`, alinhando o módulo Public à mesma origem de dados já usada em Adoption > CSM Account
  - inclusão de filtro local por `customer_id` com fallback entre as chaves `client_id`, `customer_id` e `company_id`
  - manutenção do fallback SQL direto como contingência, reduzindo divergência entre os dois fluxos e corrigindo o retorno vazio que deixava os filtros sem opções

2026-09-24: Ajustes no backend de Tasks > LCI Viability

- backend/app/tasks/lci_viability_service.py
  - remoção da regra que exigia vínculo obrigatório com projeto para salvar uma task em `IN PROGRESS`
  - manutenção do vínculo opcional com projeto existente (`project_id`) ou novo projeto (`new_project_ov` + `new_project_name`) quando informado pelo usuário
  - ajuste da validação para permitir salvar sem projeto e validar apenas o caso de criação incompleta de novo projeto

2026-09-24: Ajustes no backend de Tasks > Reports

- backend/app/tasks/report_service.py
  - migração da origem dos filtros de `Tasks > Reports` para a `vwTask`, por meio dos novos métodos do `TaskRepository`
  - remoção da obrigatoriedade de `owner_ids` para calcular opções dinâmicas e carregar o report, permitindo iniciar a filtragem por qualquer dimensão
  - manutenção da reclassificação de status no backend antes de devolver a lista de `statuses` e antes de aplicar o filtro final do report
  - preservação da carga final das rows completas via `get_task(...)`, usando a `vwTask` apenas como fonte leve/intermediária para descobrir `task_id` e montar opções de filtro
  - correção do payload de `Task Details` para normalizar o status das atividades com fallback entre `activity_status_reclassified`, `activity_status_name` e `No status`
  - ajuste do gráfico de pizza e da coluna `Status` do bloco `Schedule` para usar a mesma label efetivamente exibida para cada activity
  - reforço da serialização da lista `activities` para evitar ausência visual de status quando a tabela `tbTaskActivity` não trouxer `activity_status_name` preenchido
  - complemento da correção com lookup em `tbStatusType`, convertendo `activity_status` numérico para nome legível antes dos fallbacks; isso elimina casos em que o frontend exibia `No status` apesar de a activity possuir status ID válido
  - ampliação do payload de `schedule` para incluir `task_ws` na linha principal e `activity_ws` nas linhas filhas, além de ajustar a numeração base para o padrão visual desejado (`1` para task, atividades a partir de `2`)

- backend/app/tasks/router.py
  - ajuste dos schemas `ReportFilterOptionsRequest` e `ReportTasksRequest` para aceitar `owner_ids` opcional
  - atualização da documentação inline dos endpoints `/reports/owners` e `/reports/filter-options` para refletir a nova derivação dos filtros a partir da `vwTask`

2026-09-24: Ajustes no backend de Adoption > Cisco LCI > Cisco LCI Report

- backend/app/adoption/cisco_lci_service.py
  - alinhamento do cálculo de `Lost` do Cisco LCI Report às regras do CPI Adopt, deixando de depender exclusivamente de `lci_stage_status_id = 6` nos KPIs principais
  - reutilização da lógica do CPI Adopt para identificar tasks perdidas no funil com base em opt-out, expiração, child activities abertas expiradas e cenário em que todas as activities estão canceladas/expiradas sem conclusão
  - alteração de `get_lci_summary()` e `get_lci_report_data()` para calcular `total_lost_stages`/`fin_lost` a partir das tasks classificadas como lost pelo motor do CPI Adopt
  - ajuste de `get_lci_burnup()` para acumular `lost` pelo `task_end` das tasks lost do CPI Adopt e evitar dupla contagem dessas tasks como pipeline
  - ajuste de `get_lci_yoy()` para calcular `lost` por FY com base nas tasks lost do CPI Adopt
  - ajuste de `get_lci_stage_rows(..., "lost")` para exibir estágios pertencentes a tasks classificadas como lost segundo a regra do CPI Adopt
  - correção do import interno do serviço CPI Adopt com fallback entre `app.cisco...` e `backend.app.cisco...`, eliminando falha silenciosa de carregamento da regra de Lost
  - correção adicional do recorte de FY do `Lost` no Cisco LCI: a classificação de tasks perdidas continua vindo do CPI Adopt, mas a seleção final passou a respeitar o universo de tasks/stages exibido no FY do LCI, evitando zerar o card quando o `task_end` do CPI cair em FY diferente do estágio LCI mostrado na tela

2026-09-25: Alterações no backend do importador Forecast PMO

- src/services/importers/import_forecast_pmo.py
  - substituição do conteúdo em VBA copiado por uma implementação Python funcional do importador Forecast PMO
  - leitura de arquivo XLSX com detecção dinâmica do cabeçalho
  - processamento em chunks com remoção das linhas já lidas do arquivo de entrada
  - geração de log de execução e de arquivo XLSX com linhas com erro/advertência
  - integração com ProjectRepository.insert() para gravação de projeto e sincronização de OVs
  - tratamento de múltiplas linhas da mesma OV por conta de TP, reutilizando o mesmo project_id
  - sincronização de tbProjectType e tbProjectTeam
  - bloqueio em grupo por OV quando o cliente não existe em tbCompanyListName
  - tratamento de pessoas não encontradas em tbPersonListName como advertência operacional, sem impedir a importação do projeto
  - encerramento de alocações ativas da equipe quando o projeto vier como Closed/Canceled com data de fechamento

2026-09-26: Ajustes no backend do importador PXP Activity 5890

- src/services/importers/pxp_import_activity_5890.py
  - reforço da busca de activity por `activity_ws` com rastreabilidade no log de execução, registrando quantidade de matches e `activity_ids` encontrados a cada lookup por WS
  - alteração da resolução de activity para tratar `activity_ws` duplicado como erro explícito da linha, em vez de seguir silenciosamente com o primeiro registro encontrado
  - remoção do comportamento silencioso em falhas de busca por `activity_ws`, fazendo a linha falhar antes de seguir para `insert` sem garantia de unicidade
  - criação do helper `_insert_activity_with_ws_validation(...)` para revalidar `activity_ws` imediatamente antes do insert e confirmar após o insert que a activity criada ficou reencontrável pelo mesmo WS
  - troca do fluxo de criação para usar a validação pós-insert por `activity_ws`, reduzindo risco de inserts duplicados quando a activity já existir ou quando a persistência não puder ser confirmada

2026-09-26: Ajustes no backend do importador PXP Task 19849

- src/services/importers/pxp_import_task_19849.py
  - adição do histórico `STATUS CHANGE` para mudanças efetivas de `activity_status`, com helper dedicado para mapear o status numérico para texto legível em `tbTaskRecord`
  - gravação de histórico `STATUS CHANGE` ao cancelar child activities por `Opted Out`
  - gravação de histórico `STATUS CHANGE` no momento da criação automática da activity `Implement`
  - inclusão de correção automática de `activity_seq` das child activities existentes conforme a ordem padrão do LCI: `Onboard=1`, `Implement=2`, `Use=3`, `Engage=4`, `Adopt=5`
  - atualização do fluxo `_update_activity_from_stage(...)` para corrigir sequenciamento inconsistente e registrar histórico textual quando houver ajuste de sequência, scope ou completed

2026-09-26: Alterações no backend do importador New Cisco EA

- src/services/importers/new_cisco_ea.py
  - substituição do conteúdo VBA copiado por uma implementação Python funcional do importador New Cisco EA
  - leitura de arquivo de entrada em `xlsx` e `csv`
  - processamento em chunks com remoção das linhas já lidas do arquivo original
  - geração de log de execução e de arquivo XLSX com linhas com falha
  - validação das colunas obrigatórias e normalização dos campos principais da importação
  - resolução de customer priorizando `WEB ORDER ID` via `vwCiscoEACustomerWebOrder`, com fallback por `END CUSTOMER NAME`
  - remoção da obrigatoriedade prática de `END CUSTOMER NAME` para resolver customer quando a `WEB ORDER ID` já apontar um cliente válido
  - ajuste da mensagem de erro para cobrir falha nas duas estratégias de resolução de cliente
  - correção da resolução por `END CUSTOMER NAME` para tentar também a variante em caixa alta e com espaços internos normalizados, compatibilizando a busca com `tbCompanyListName`
  - ajuste defensivo de truncamento de `CCW LINE STATUS` para 25 caracteres, evitando erro SQL `Data too long for column 'ea_ccw_line_status'`
  - resolução ou criação automática de produto Cisco em `tbProduct` a partir de `ORDERED ITEM`
  - deduplicação lógica de registros EA por `ea_web_order_id + ea_product_id + ea_subscription_id + ea_end_customer_id + ea_magic_key`
  - criação e atualização de registros em `tbCiscoEA`
  - criação automática de task tipo `1` para casos elegíveis, com owner resolvido via `resolve_csm(...)`
  - preenchimento de `task_track` por produto (`5533 -> EA3-M` / `5194 -> ELA2-M`) e `task_subtrack` com `ORDERED ITEM`
  - gravação de histórico `"Task created."` em `tbTaskRecord`
  - propagação do `ea_new_task_id` para registros irmãos por customer + subscription, ou customer + web order quando a subscription vier nula

- backend/app/modules/public_service.py
  - inclusão de dois Novos importadores no catálogo: CiscoNewEA e ForecastPMO
2026-09-26: Ajuste de UX no log do importador Forecast PMO

- src/services/importers/import_forecast_pmo.py
  - inclus�o de log consolidado por linha processada no arquivo .log, com ow, ov e esult`r
  - diferencia��o expl�cita entre success, warning, error e ignored`r
  - inclus�o de project_id, created, updated e quantidade de warnings quando aplic�vel
  - registro do motivo resumido em warnings/erros/linhas ignoradas para facilitar troubleshooting operacional

- docs/05_deployment/importer/import_forecast_pmo.md
  - atualiza��o da documenta��o com o novo padr�o de logging por linha e exemplos esperados no arquivo de execu��o
